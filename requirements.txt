Flask>=3.0
Flask-Cors>=4.0
Flask-SocketIO>=5.3
python-socketio>=5.9
eventlet>=0.35  # for `gunicorn -k eventlet` in production
pyserial>=3.5   # USB-CDC bridge for the XIAO ESP32 receiver
pytest>=8.0    # dev only
